version: "3"
services:
  client:
    build: client/
    ports:
      - "3000:3000"
      - "35729:35729"
    environment:
      - CHOKIDAR_USEPOLLING=true
    volumes:
      - ./client:/app
      - client-deps:/app/node_modules
volumes:
  ? client-deps
  ? server-deps
