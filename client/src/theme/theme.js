import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
    palette: {
        primary: {
            main: '#121316',
        },
        secondary: {
            main: "#f22b29"
        },
        error: {
            main: "#545454"
        }
    },
});

export default theme